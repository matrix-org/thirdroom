import { createCursorBuffer } from "../allocator/CursorBuffer";

export const gameBuffer = createCursorBuffer();
export const renderableBuffer = createCursorBuffer();
export const hierarchyBuffer = createCursorBuffer();
