// import { defineQuery } from "bitecs";

// import { GameContext } from "../GameTypes";
// import { getModule } from "../module/module.common";
// import { TransferAuthority } from "./NetworkComponents";
// import { NetworkModule } from "./network.game";

// const transferAuthorityQuery = defineQuery([TransferAuthority]);

// export function TransferAuthoritySystem(ctx: GameContext) {
//   const network = getModule(ctx, NetworkModule);

//   const ents = transferAuthorityQuery(ctx.world);
//   for (let i = 0; i < ents.length; i++) {
//     const eid = ents[i];
//   }
// }
